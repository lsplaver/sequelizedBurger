<div class="newBody">
    <h1>Eat-Da-Burger</h1>
    <div>
        <div class="uneaten">
            <ul>
                <label>Uneaten Burgers</label>
                {{#each burgers}} {{#unless this.data.devoured}}
                <li>
                    <!-- {{ id }} {{ burger_name }} {{ devoured }} -->
                    <!-- {{burgers.id}} {{burgers.burger_name}} {{burgers.devoured}} -->
                    ID: {{ this.data.id }} Burger Name: {{ this.data.burger_name }} Devoured: {{ this.data.devoured }}
                    <button class="isEaten" data-id="{{this.data.id}}" data-waseaten="true">Devour It!</button>
                </li>
                {{/unless}} {{/each}}
            </ul>
        </div>
        <div class="eaten">
            <ul>
                <label>Already Eaten Burgers</label>
                {{#each burgers}} {{#if this.data.devoured}}
                <li>
                    ID: {{ this.data.id }} Burger Name: {{ this.data.burger_name }} Devoured: {{ this.data.devoured }}
                </li>
                {{/if}} {{/each}}
            </ul>
        </div>
        <div class="newBurger">
            <form class="newForm">
                <br>
                <div>
                    <label for="inputBurger">Enter the name of your burger here:</label>
                    <input type="text" id="inputBurger" name="burgerName">
                </div>
                <br>
                <div>
                    <label for="btnSubmit">Submit your order here:</label>
                    <button id="btnSubmit" type="submit">Submit</button>
                </div>
            </form>
        </div>
    </div>
</div>